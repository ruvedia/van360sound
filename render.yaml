services:
  - type: web
    name: van360sound-frontend
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: node frontend/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        fromService:
          type: web
          name: van360sound-backend
          property: url
      - key: BACKEND_URL
        fromService:
          type: web
          name: van360sound-backend
          property: url

  - type: web
    name: van360sound-backend
    runtime: python
    region: oregon
    plan: free
    buildCommand: bash ./backend/render-build.sh
    startCommand: cd backend && gunicorn van360sound.wsgi:application --bind 0.0.0.0:${PORT:-10000} --workers 1 --log-level debug --access-logfile -
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DJANGO_SETTINGS_MODULE
        value: van360sound.settings
      - key: DJANGO_DEBUG
        value: 'True'
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 1
